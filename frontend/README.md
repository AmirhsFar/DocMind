# Frontend

Intentionally empty for now — this gets scaffolded in Phase 5 as a small
React + Vite chat UI (login, file upload, streaming chat with cited sources).

The backend is the point of this project; the frontend only needs to be
enough to prove the API works end to end.
